{
  "name": "saxo-sample-request",
  "version": "1.0.0",
  "private": true,
  "description": "Local full-stack sample request application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
